# Output
output_dir: /lustre/fswork/projects/rech/ywa/uds37kc/GTH/results/mamba2_130m_hf

# Optimization
learning_rate: 3.0e-4
weight_decay: 0.1
adam_beta2: 0.95
warmup_ratio: 0.1
max_grad_norm: 1.0

# Batching
train_batch_size: 8
gradient_accumulation_steps: 8
dataloader_num_workers: 4
dataloader_prefetch_factor: 2

# Sequence
block_size: 1024

# Evaluation / checkpointing
eval_steps: 200
save_steps: 5000
logging_steps: 200